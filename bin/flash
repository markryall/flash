#!/usr/bin/env ruby

$: << File.dirname(__FILE__)+'/../lib'

require 'flash/corpus'
include Flash::Corpus

loop do
  begin
    phrase.each do |section|
      print section
      gets
    end
    puts
  rescue Interrupt
    exit
  end
end