#!/usr/bin/env ruby

$: << File.dirname(__FILE__)+'/../lib'

require 'flash/corpus'

corpus = Flash::Corpus.new ARGV.shift

loop do
  begin
    corpus.phrase.each do |section|
      print section
      gets
    end
    puts
  rescue Interrupt
    exit
  end
end